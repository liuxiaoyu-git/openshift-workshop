config:
    renderer: adoc

modules:
  kafka:
    name: Install a Kafka cluster
  operators_quay:
    name: Configure Quay
  operators_go:
    name: Deploy a Go Operator
  operators_ansible:
    name: Deploy an Ansible Operator
